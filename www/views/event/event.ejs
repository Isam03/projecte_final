<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test view</title>
</head>
<body>
    <h1>Event view</h1>
    <% if (event != null) { %>
        <%= "Titulo: " + event.titulo  %>
        <br>
        <%= event.descripcion %>
        <br>
        <% if(categoria != null) { %> 
            <% if (event.id_categoria == categoria._id) { %>
                <%= categoria.titulo %>
            <% } %>
        <% } %>
        <br>
        <%= event.fechahora %>
        <br>
        <%= event.precio %>
        <br>
        <%= event.correoContacto %>
        <br>
        <%= event.telefonoContacto %>
        <br>
        <%= event.creado_por %>
        <br>
        <%= event.coords %>
        <br>
    <% } %>
</body>
</html>